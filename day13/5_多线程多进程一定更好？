1、多进程对变量的引用是独立的，也就是各自拥有不同的数据内存区，互不干扰，如同一个全局变量globalValue，对他们而言也是各自拥有一个globalValue，两者的值乎不干扰
2、因为两者的值互不干扰，在有些场景需要两者进行通信互动（基于一个变量），因此就有了线程的出现，线程变量内存区域共同使用
3、多线程与多进程并不会一定比单进程与单线程好：
    首先第一个，多线程与多进程切换任务任务时时需要一点时间，当达到一定数量，效率会降低
    然后第二个，密集计算型任务，需要的是集中的计算能力，频繁切换会降低效率，但是对于io类如web任务，等待时间比较长，切换的时间可以忽略不计



1、  print(*objects, sep='  ', end='\n', file=sys.stdout, flush=False)
    第一个表示输出对象
    第二个表示分隔符，默认空格
    第三个表示末尾结束的符号，默认换行
    第四个为输出的位置，默认标准控制输出台黑窗口，可改为一个文件
    第五个为是否立即写入

2、format的使用
    print(f"{text: <16}",end="")
    == print("{0: <16}".format(text,),end="")
    == print(str(text).ljust(16," "),end="")
    第一个：    ^、<、>分别是居中、左对齐、右对齐，后面带宽度
    第二个：    :号后面带填充的字符，只能是一个字符，不指定的话默认是用空格填充

1.clone(克隆)与pull(拉取的区别)：克隆是将一个远程版本库所有文件放到本地，二拉取是部分，如某个分支，某个文件

2、pull(拉取)与fetch(获取)的区别：拉取是将远程库文件放到本地后直接合并到工作区，而fetch是放到本地库，需要再merge一下才会合并到工作区



相关参考链接：

变基：https://git-scm.com/book/zh/v2/Git-%E5%88%86%E6%94%AF-%E5%8F%98%E5%9F%BA
